import { useEffect, useMemo, useRef, useState } from 'react';
import { History, Home, Pill, UserRound } from 'lucide-react';
import type { DB, Med, Role, Tab } from './model';
import { dueFor, load } from './model';
import { AddModal, AlarmOverlay, ConfirmationOverlay, DetailSheet, EditModal, NavButton, StartTimeModal } from './components';
import { AuthGate, OnboardingGate } from './auth';
import { HistoryView, HomeView, MedsView, SettingsView } from './views';
import { ProfilePanel, ReportView } from './profile';
import { supabase } from './supabase';
import { deleteMedication, getExistingProfile, insertDose, loadCloud, subscribeCloud, updateOwnProfile, upsertMedication } from './cloud';

function clearAuthHash() {
  if (window.location.hash && /(?:access_token|refresh_token|code|error)=/i.test(window.location.hash)) {
    window.history.replaceState(null, document.title, `${window.location.pathname}${window.location.search}`);
  }
}

function App() {
  const [db, setDb] = useState<DB>(load); const [authenticated,setAuthenticated]=useState(false); const [authChecked,setAuthChecked]=useState(false);
  const [onboarded,setOnboarded]=useState(()=>!!localStorage.getItem('cm-v12-role')&&!!localStorage.getItem('cm-v12-patient-id'));
  const [role,setRole]=useState<Role>(()=>(localStorage.getItem('cm-v12-role') as Role)||'patient'); const [patientId,setPatientId]=useState(()=>localStorage.getItem('cm-v12-patient-id')||'');
  const [account,setAccount]=useState<{name:string;email:string;avatarUrl:string|null}>({name:localStorage.getItem('cm-v12-name')||'',email:'',avatarUrl:null});
  const [tab,setTab]=useState<Tab>('home'),[now,setNow]=useState(Date.now()); const [showProfile,setShowProfile]=useState(false); const [reportPeriod,setReportPeriod]=useState<'daily'|'weekly'|'monthly'>('monthly');
  const [alarm,setAlarm]=useState<{med:Med;due:Date}|null>(null),[done,setDone]=useState<any>(null); const [showAdd,setShowAdd]=useState(false),[detail,setDetail]=useState<Med|null>(null),[edit,setEdit]=useState<Med|null>(null),[startMed,setStartMed]=useState<Med|null>(null),[snoozes,setSnoozes]=useState<Record<string,number>>({});
  const audio=useRef<AudioContext|null>(null),timer=useRef<number|null>(null),refreshLock=useRef(false);

  async function hydrateSession(){
    const existing=await getExistingProfile();
    if(existing){
      localStorage.setItem('cm-v12-role',existing.role); localStorage.setItem('cm-v12-patient-id',existing.patientId); localStorage.setItem('cm-v12-name',existing.name);
      setRole(existing.role); setPatientId(existing.patientId); setOnboarded(true); setAccount({name:existing.name,email:existing.email,avatarUrl:existing.avatarUrl});
    }else{
      const {data:{user}}=await supabase.auth.getUser();
      const fallback=String(user?.user_metadata?.full_name||user?.user_metadata?.name||user?.email?.split('@')[0]||'Usuário');
      setAccount({name:localStorage.getItem('cm-v12-name')||fallback,email:user?.email||'',avatarUrl:String(user?.user_metadata?.avatar_url||user?.user_metadata?.picture||'')||null});
      localStorage.removeItem('cm-v12-role'); localStorage.removeItem('cm-v12-patient-id'); setOnboarded(false); setPatientId('');
    }
  }

  useEffect(()=>{let mounted=true;supabase.auth.getSession().then(async({data})=>{if(!mounted)return;setAuthenticated(!!data.session);clearAuthHash();if(!data.session){localStorage.removeItem('cm-v12-authenticated');localStorage.removeItem('cm-v12-role');localStorage.removeItem('cm-v12-patient-id');setOnboarded(false);setPatientId('');setAccount({name:'',email:'',avatarUrl:null});}else{localStorage.setItem('cm-v12-authenticated','1');try{await hydrateSession()}catch(e){console.error(e)}}setAuthChecked(true)});const{data:listener}=supabase.auth.onAuthStateChange(async(_event,session)=>{setAuthenticated(!!session);clearAuthHash();if(session){localStorage.setItem('cm-v12-authenticated','1');try{await hydrateSession()}catch(e){console.error(e)}}else{localStorage.removeItem('cm-v12-authenticated');localStorage.removeItem('cm-v12-role');localStorage.removeItem('cm-v12-patient-id');setOnboarded(false);setPatientId('');setAccount({name:'',email:'',avatarUrl:null});stopSound()}});return()=>{mounted=false;listener.subscription.unsubscribe()}},[]);
  useEffect(()=>{const id=window.setInterval(()=>setNow(Date.now()),1000);return()=>clearInterval(id)},[]);
  async function refresh(id=patientId){if(!id||refreshLock.current)return;refreshLock.current=true;try{setDb(await loadCloud(id))}catch(e){console.error(e)}finally{refreshLock.current=false}}
  useEffect(()=>{if(authenticated&&onboarded&&patientId)refresh()},[authenticated,onboarded,patientId]); useEffect(()=>{if(!authenticated||!onboarded||!patientId)return subscribeCloud(patientId,()=>refresh())},[authenticated,onboarded,patientId]);
  const schedule=useMemo(()=>db.meds.map(m=>({m,due:dueFor(m,db.events)})).filter((x):x is{m:Med;due:Date}=>!!x.due).sort((a,b)=>+a.due-+b.due),[db]); const pending=schedule.filter(x=>x.due.getTime()<=now&&(snoozes[x.m.id]||0)<=now)[0]; const upcoming=schedule.filter(x=>x.due.getTime()>now),next=upcoming[0];
  useEffect(()=>{if(!authenticated||!onboarded||!patientId||!pending||alarm||done)return;setAlarm({med:pending.m,due:pending.due});startSound();if('Notification'in window&&Notification.permission==='granted'){try{new Notification('🔔 Hora do medicamento',{body:pending.m.name})}catch{}}},[authenticated,onboarded,patientId,pending?.m.id,pending?.due?.getTime(),alarm,done]);
  function startSound(){try{const C=window.AudioContext||(window as any).webkitAudioContext;if(!C)return;stopSound();const c=new C();audio.current=c;const beep=()=>{if(c.state==='suspended')c.resume().catch(()=>{});const o=c.createOscillator(),g=c.createGain();o.type='square';o.frequency.value=880;g.gain.value=.16;o.connect(g);g.connect(c.destination);o.start();window.setTimeout(()=>{try{o.stop()}catch{}},500)};beep();timer.current=window.setInterval(beep,1100)}catch{}}
  function stopSound(){if(timer.current)clearInterval(timer.current);timer.current=null;audio.current?.close();audio.current=null}
  async function confirmMed(m:Med){const item=schedule.find(x=>x.m.id===m.id);if(!item||!patientId)return;stopSound();try{const row=await insertDose(patientId,m.id,item.due.toISOString(),role);const e={id:row.id,medId:m.id,scheduled:row.scheduled_at,confirmed:row.confirmed_at,by:role==='patient'?'Paciente':'Acompanhante'} as DB['events'][number];const nextDate=new Date(new Date(row.confirmed_at).getTime()+m.interval*60000);setAlarm(null);setSnoozes(s=>({...s,[m.id]:0}));await refresh();setDone({med:m,event:e,next:nextDate})}catch(e:any){alert(e?.message||'Não foi possível confirmar esta dose.');await refresh()}}
  function snooze(m:Med,min:number){stopSound();setSnoozes(s=>({...s,[m.id]:Date.now()+min*60000}));setAlarm(null)}
  async function setFirstTime(iso:string){if(!startMed||!patientId)return;try{await upsertMedication(patientId,{...startMed,start:iso,active:true});await refresh();setStartMed(null)}catch(e:any){alert(e?.message||'Não foi possível salvar o horário.')}}
  async function pause(m:Med){if(!patientId)return;try{await upsertMedication(patientId,{...m,active:!m.active});await refresh();setDetail(null)}catch(e:any){alert(e?.message||'Não foi possível alterar o medicamento.')}}
  async function addMed(data:any){if(!patientId)return;const m:Med={id:crypto.randomUUID(),name:data.name,interval:data.interval,start:null,active:true,form:data.form,dose:data.dose,note:data.note,photo:data.photo};try{await upsertMedication(patientId,m);await refresh();setShowAdd(false)}catch(e:any){alert(e?.message||'Não foi possível adicionar.')}}
  async function updateMed(m:Med){if(!patientId)return;try{await upsertMedication(patientId,m);await refresh();setEdit(null);setDetail(null)}catch(e:any){alert(e?.message||'Não foi possível salvar.')}}
  async function removeMed(m:Med){if(!patientId||!confirm(`Excluir ${m.name}?`))return;try{await deleteMedication(patientId,m.id);await refresh();setDetail(null);setEdit(null)}catch(e:any){alert(e?.message||'Não foi possível excluir.')}}
  function askNotifications(){if(!('Notification'in window))return alert('Este navegador não oferece notificações.');Notification.requestPermission().then(()=>setDb(x=>({...x,settings:{...x.settings,notifications:Notification.permission==='granted'}})))} function testAlarm(){startSound();window.setTimeout(stopSound,5000)}
  async function saveProfile(name:string){const clean=await updateOwnProfile(name,role,patientId);setAccount(a=>({...a,name:clean}));setDb(d=>({...d,patientName:role==='patient'?clean:d.patientName}));}
  async function signOut(){stopSound();setShowProfile(false);await supabase.auth.signOut()}
  if(!authChecked)return <div className="grid min-h-screen place-items-center bg-[#f5f7fb] text-slate-500">Carregando…</div>; if(!authenticated)return <AuthGate onComplete={(r,name)=>{localStorage.setItem('cm-v12-name',name);setAccount(a=>({...a,name}));setRole(r);setAuthenticated(true)}}/>; if(!onboarded)return <OnboardingGate db={db} onComplete={(r,name,id)=>{localStorage.setItem('cm-v12-role',r);localStorage.setItem('cm-v12-patient-id',id);localStorage.setItem('cm-v12-name',name);setRole(r);setPatientId(id);setAccount(a=>({...a,name}));setOnboarded(true)}}/>;
  const activePending=alarm||(!done&&pending?{med:pending.m,due:pending.due}:null); const userName=account.name||localStorage.getItem('cm-v12-name')||db.patientName||'Usuário';
  return <div className="min-h-screen bg-[#f5f7fb] text-[#182033]"><header className="border-b border-slate-100 bg-white"><div className="mx-auto flex max-w-xl items-center justify-between px-5 py-5"><button onClick={()=>setShowProfile(true)} className="flex min-w-0 items-center gap-3 text-left" aria-label="Abrir perfil"><div className="grid h-12 w-12 shrink-0 place-items-center overflow-hidden rounded-2xl bg-blue-50 text-blue-600">{account.avatarUrl?<img src={account.avatarUrl} alt="" className="h-full w-full object-cover"/>:<Pill size={27}/>}</div><div className="min-w-0"><div className="text-[10px] font-bold tracking-[.28em] text-slate-400">CONTROLE DE</div><h1 className="text-[27px] font-medium leading-none tracking-tight">Medicamentos</h1><div className="mt-2 flex items-center gap-1.5 truncate text-sm text-slate-500"><UserRound size={14}/> {userName}</div></div></button><button onClick={()=>setShowProfile(true)} className="grid h-11 w-11 shrink-0 place-items-center overflow-hidden rounded-2xl bg-blue-50 text-blue-600" aria-label="Abrir perfil">{account.avatarUrl?<img src={account.avatarUrl} alt="" className="h-full w-full object-cover"/>:<UserRound size={21}/>}</button></div></header><main className="mx-auto max-w-xl px-5 pb-28 pt-5">{tab==='home'&&<HomeView db={db} activePending={activePending} next={next} upcoming={upcoming} now={now} onDetail={setDetail}/>} {tab==='meds'&&<MedsView db={db} onAdd={()=>setShowAdd(true)} onDetail={setDetail} onPause={pause} onStart={setStartMed}/>} {tab==='history'&&<HistoryView db={db}/>} {tab==='settings'&&<SettingsView db={db} onNotifications={askNotifications} onAlarm={testAlarm}/>} {tab==='report'&&<ReportView db={db} period={reportPeriod} onBack={()=>{setTab('home');setShowProfile(true)}}/>}</main><nav className="fixed bottom-0 left-0 right-0 z-30 border-t border-slate-100 bg-white/95 px-2 pb-[max(8px,env(safe-area-inset-bottom))] pt-2 backdrop-blur"><div className="mx-auto grid max-w-xl grid-cols-4 gap-1"><NavButton active={tab==='home'} icon={<Home size={22}/>} label="Início" onClick={()=>setTab('home')}/><NavButton active={tab==='meds'} icon={<Pill size={22}/>} label="Medicamentos" onClick={()=>setTab('meds')}/><NavButton active={tab==='history'} icon={<History size={22}/>} label="Histórico" onClick={()=>setTab('history')}/><NavButton active={false} icon={<UserRound size={22}/>} label="Perfil" onClick={()=>setShowProfile(true)}/></div></nav>{activePending&&<AlarmOverlay med={activePending.med} due={activePending.due} now={now} label={role==='patient'?'USADO':'ADMINISTRADO'} onConfirm={()=>confirmMed(activePending.med)} onSnooze={m=>snooze(activePending.med,m)}/>} {done&&<ConfirmationOverlay done={done} onClose={()=>setDone(null)} onNext={()=>{setDone(null);setTab('home')}}/>} {detail&&<DetailSheet med={detail} events={db.events} now={now} onClose={()=>setDetail(null)} onConfirm={()=>{const due=dueFor(detail,db.events);if(due&&due.getTime()<=now)confirmMed(detail)}} onPause={()=>pause(detail)} onEdit={()=>setEdit(detail)} onDelete={()=>removeMed(detail)}/>} {edit&&<EditModal med={edit} onClose={()=>setEdit(null)} onSave={updateMed}/>} {startMed&&<StartTimeModal med={startMed} now={now} onClose={()=>setStartMed(null)} onSave={setFirstTime}/>} {showAdd&&<AddModal onClose={()=>setShowAdd(false)} onSave={addMed}/>} {showProfile&&<ProfilePanel db={db} role={role} name={userName} email={account.email} avatarUrl={account.avatarUrl} onClose={()=>setShowProfile(false)} onSettings={()=>{setShowProfile(false);setTab('settings')}} onReport={p=>{setReportPeriod(p);setShowProfile(false);setTab('report')}} onSaveProfile={saveProfile} onSignOut={signOut}/>}</div>;
}
export default App;

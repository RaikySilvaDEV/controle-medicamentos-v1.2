import { useState } from 'react';
import { Check, Pencil, Pause, Pill, Play, ShieldAlert, Trash2, X } from 'lucide-react';
import type { Event, Med } from './model';
import { dueFor, duration, fmt } from './model';

export function NavButton({ active, icon, label, onClick }: { active: boolean; icon: React.ReactNode; label: string; onClick: () => void }) { return <button onClick={onClick} className={`flex flex-col items-center justify-center gap-1 rounded-2xl py-2.5 text-[11px] font-bold ${active ? 'bg-blue-50 text-blue-600' : 'text-slate-500'}`}>{icon}<span>{label}</span></button>; }

export function AlarmOverlay({ med, due, now, label, onConfirm, onSnooze }: { med: Med; due: Date; now: number; label: string; onConfirm: () => void; onSnooze: (m: number) => void }) { return <div className="fixed inset-0 z-[60] flex items-end justify-center bg-red-950/45 p-3 sm:items-center"><div className="w-full max-w-md rounded-[32px] bg-gradient-to-br from-red-600 to-red-500 p-6 text-white shadow-2xl"><div className="flex justify-center"><div className="grid h-20 w-20 place-items-center rounded-full bg-white/15 ring-8 ring-white/5"><ShieldAlert size={47}/></div></div><div className="mt-5 text-center text-xs font-extrabold tracking-[.18em]">HORA DO MEDICAMENTO!</div><h2 className="mt-2 text-center text-3xl font-black leading-tight">{med.name}</h2><div className="mt-5 rounded-2xl bg-red-800/35 p-4 text-center"><span className="text-sm opacity-90">Deveria ter sido às</span><strong className="mt-1 block text-3xl">{fmt(due)}</strong><span className="mt-3 block text-sm opacity-90">Atrasado há</span><strong className="block text-2xl">{duration(Math.max(0, now - due.getTime()))}</strong></div><button onClick={onConfirm} className="mt-4 flex w-full items-center justify-center gap-2 rounded-2xl bg-white py-4 font-black text-red-600 shadow-xl"><Check size={22}/> CONFIRMAR QUE FOI {label}</button><div className="mt-3 grid grid-cols-2 gap-3"><button onClick={() => onSnooze(5)} className="rounded-2xl border border-white/60 py-3 font-bold">ADIAR 5 MIN</button><button onClick={() => onSnooze(10)} className="rounded-2xl border border-white/60 py-3 font-bold">ADIAR 10 MIN</button></div></div></div>; }

export function ConfirmationOverlay({ done, onClose, onNext }: { done: { med: Med; event: Event; next: Date }; onClose: () => void; onNext: () => void }) { return <div className="fixed inset-0 z-[65] flex items-end justify-center bg-emerald-950/25 p-3 sm:items-center"><div className="w-full max-w-md rounded-[32px] bg-white p-6 shadow-2xl"><div className="flex justify-end"><button onClick={onClose} className="rounded-full bg-slate-100 p-2"><X size={20}/></button></div><div className="flex flex-col items-center text-center"><div className="grid h-20 w-20 place-items-center rounded-full bg-emerald-500 text-white shadow-lg"><Check size={48}/></div><div className="mt-5 text-2xl font-black text-emerald-700">Medicamento confirmado!</div><p className="mt-1 text-slate-500">Registro salvo com sucesso.</p><div className="mt-5 w-full rounded-2xl bg-slate-50 p-4 text-left"><b className="text-lg">{done.med.name}</b><div className="mt-3 grid grid-cols-2 gap-3 text-sm"><div><span className="block text-slate-400">Previsto</span><strong className="text-red-600">{fmt(done.event.scheduled)}</strong></div><div><span className="block text-slate-400">Confirmado</span><strong className="text-emerald-600">{fmt(done.event.confirmed)}</strong></div></div></div><div className="mt-4 w-full rounded-2xl bg-emerald-50 p-4 text-left"><span className="text-sm text-emerald-700">Próximo horário</span><b className="mt-1 block text-lg">{done.med.name}</b><strong className="mt-1 block text-2xl text-emerald-700">{fmt(done.next)}</strong></div><button onClick={onNext} className="mt-5 w-full rounded-2xl bg-emerald-600 py-3.5 font-black text-white">CONTINUAR</button></div></div></div>; }

export function DetailSheet({ med, events, now, onClose, onConfirm, onPause, onEdit, onDelete }: { med: Med; events: Event[]; now: number; onClose: () => void; onConfirm: () => void; onPause: () => void; onEdit: (m: Med) => void; onDelete: () => void }) {
  const due = dueFor(med, events); const last = [...events].filter(e => e.medId === med.id).sort((a, b) => +new Date(b.confirmed) - +new Date(a.confirmed))[0]; const overdue = !!due && +due <= now;
  return <div className="fixed inset-0 z-50 flex items-end justify-center bg-slate-950/35 p-3 sm:items-center"><div className="max-h-[88vh] w-full max-w-md overflow-auto rounded-[30px] bg-white p-6 shadow-2xl"><div className="flex items-center justify-between"><div className="text-[10px] font-extrabold tracking-[.22em] text-slate-400">DETALHES DO MEDICAMENTO</div><button onClick={onClose} className="rounded-full bg-slate-100 p-2"><X size={19}/></button></div><div className="mt-5 flex items-center gap-4"><div className="grid h-16 w-16 shrink-0 place-items-center overflow-hidden rounded-2xl bg-blue-50 text-blue-600">{med.photo ? <img src={med.photo} alt="Medicamento" className="h-full w-full object-cover"/> : <Pill size={31}/>}</div><div className="min-w-0"><h2 className="text-2xl font-bold leading-tight">{med.name}</h2><p className="mt-1 text-sm text-slate-500">{med.form || 'Medicamento'} • {med.dose || '1 dose'}</p></div></div><div className="mt-6 grid grid-cols-2 gap-3"><InfoBox label="Intervalo" value={med.interval >= 1440 ? `A cada ${med.interval / 1440} dias` : med.interval >= 60 ? `A cada ${med.interval / 60} horas` : `A cada ${med.interval} min`}/><InfoBox label="Situação" value={med.active ? 'Em uso' : 'Pausado'}/><InfoBox label="Próximo" value={due ? fmt(due) : 'Aguardando'}/><InfoBox label="Última confirmação" value={last ? fmt(last.confirmed) : 'Nenhuma'}/></div>{overdue && <div className="mt-4 rounded-2xl bg-red-50 p-4 text-sm font-bold text-red-700">🔴 Esta dose está atrasada e permanece pendente até ser confirmada.</div>}{med.note && <div className="mt-4 rounded-2xl bg-blue-50 p-4 text-sm text-blue-900"><b>Observação</b><p className="mt-1">{med.note}</p></div>}<div className="mt-5 grid grid-cols-2 gap-3"><button onClick={onPause} className="flex items-center justify-center gap-2 rounded-2xl border border-slate-200 py-3 font-bold">{med.active ? <Pause size={18}/> : <Play size={18}/>} {med.active ? 'Pausar' : 'Retomar'}</button><button onClick={() => onEdit(med)} className="flex items-center justify-center gap-2 rounded-2xl bg-blue-600 py-3 font-bold text-white"><Pencil size={18}/> Editar</button></div>{overdue && <button onClick={onConfirm} className="mt-3 flex w-full items-center justify-center gap-2 rounded-2xl bg-red-600 py-3.5 font-black text-white"><Check size={19}/> CONFIRMAR USO</button>}<button onClick={onDelete} className="mt-3 flex w-full items-center justify-center gap-2 rounded-2xl py-3 font-bold text-red-600"><Trash2 size={18}/> Excluir medicamento</button></div></div>;
}
function InfoBox({ label, value }: { label: string; value: string }) { return <div className="rounded-2xl bg-slate-50 p-3"><span className="block text-xs text-slate-400">{label}</span><b className="mt-1 block text-sm">{value}</b></div>; }

export function AddModal({ onClose, onSave }: { onClose: () => void; onSave: (name: string, interval: number, form: string, dose: string, note: string) => void }) { const [name, setName] = useState(''); const [interval, setIntervalValue] = useState(2); const [form, setForm] = useState('Colírio'); const [dose, setDose] = useState('1 gota'); const [note, setNote] = useState(''); return <div className="fixed inset-0 z-50 flex items-end justify-center bg-slate-950/35 p-3 sm:items-center"><div className="w-full max-w-md rounded-[30px] bg-white p-6 shadow-2xl"><div className="flex items-center justify-between"><div><div className="text-[10px] font-bold tracking-[.2em] text-slate-400">NOVO</div><h2 className="text-2xl font-bold">Adicionar medicamento</h2></div><button onClick={onClose} className="rounded-full bg-slate-100 p-2"><X size={19}/></button></div><label className="mt-5 block text-sm font-bold">Nome<input value={name} onChange={e => setName(e.target.value)} className="mt-1 w-full rounded-2xl border border-slate-200 p-3 outline-none focus:border-blue-500" placeholder="Nome do medicamento"/></label><div className="mt-3 grid grid-cols-2 gap-3"><label className="text-sm font-bold">Intervalo<input type="number" min={1} value={interval} onChange={e => setIntervalValue(Math.max(1, Number(e.target.value)))} className="mt-1 w-full rounded-2xl border border-slate-200 p-3"/></label><label className="text-sm font-bold">Unidade<select value="hour" className="mt-1 w-full rounded-2xl border border-slate-200 p-3"><option value="hour">Horas</option></select></label></div><label className="mt-3 block text-sm font-bold">Forma<select value={form} onChange={e => setForm(e.target.value)} className="mt-1 w-full rounded-2xl border border-slate-200 p-3">{['Colírio','Comprimido','Cápsula','Gotas','Pomada','Outro'].map(x => <option key={x}>{x}</option>)}</select></label><label className="mt-3 block text-sm font-bold">Dose<input value={dose} onChange={e => setDose(e.target.value)} className="mt-1 w-full rounded-2xl border border-slate-200 p-3" placeholder="1 gota / 1 comprimido"/></label><label className="mt-3 block text-sm font-bold">Observação<textarea value={note} onChange={e => setNote(e.target.value)} className="mt-1 min-h-20 w-full rounded-2xl border border-slate-200 p-3" placeholder="Ex.: aplicar 7 min após outro medicamento"/></label><button disabled={!name.trim()} onClick={() => onSave(name, interval * 60, form, dose, note)} className="mt-5 w-full rounded-2xl bg-blue-600 py-3.5 font-black text-white disabled:opacity-40">Adicionar medicamento</button></div></div>; }
